# dependencies
node_modules/

# build output
dist/

# generated by ray develop / ray build (regenerated on every build)
raycast-env.d.ts

# editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp
.zed/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (this extension reads its API key from Raycast preferences,
# not from disk — but block them anyway as a safety net)
.env
.env.*
*.pem
*.key

# private build plan kept locally, not for the public repo
PLAN.md
